PORT=8080
JWT_SECRET=mysecret

# postgres://<USER>:<PASSWORD>@<HOST>:<PORT>/<DBNAME>?sslmode=<MODE>
# HOST is where postgres service runs so our casae its db
DB_URL=postgres://postgres:postgres@db:5432/biketracker?sslmode=disable